const {
  default: RegisterForm,
} = require('components/RegisterForm/RegisterForm');

const Register = () => {
  return (
    <main>
      <RegisterForm />
    </main>
  );
};

export default Register;
